---
phase: 04-production-and-ship
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - template/docker-compose.prod.yml
  - template/.infrastructure/conf/traefik/prod/traefik.yml
  - template/.spin.yml
autonomous: true
requirements:
  - PROD-01
  - PROD-02
  - PROD-03
  - PROD-04
  - PROD-05
  - PROD-06
  - PROD-07
  - TRAF-03
  - TRAF-04
  - SPIN-10

must_haves:
  truths:
    - "Prod compose defines a Swarm-mode deployment with rolling updates and rollback"
    - "Traefik in prod uses Let's Encrypt ACME HTTP-01 for SSL certificates"
    - "FrankenPHP labels default to port 8443/scheme https in prod compose"
    - "HTTP requests redirect to HTTPS in production"
    - ".spin.yml has changeme@example.com placeholder for post-install.sh patching"
  artifacts:
    - path: "template/docker-compose.prod.yml"
      provides: "Production Swarm compose overlay"
      contains: "deploy:"
    - path: "template/.infrastructure/conf/traefik/prod/traefik.yml"
      provides: "Production Traefik config with ACME"
      contains: "letsencryptresolver"
    - path: "template/.spin.yml"
      provides: "Starter Spin configuration"
      contains: "changeme@example.com"
  key_links:
    - from: "template/docker-compose.prod.yml"
      to: "template/docker-compose.yml"
      via: "compose overlay (extends base services)"
      pattern: "services:"
    - from: "template/docker-compose.prod.yml"
      to: "template/.infrastructure/conf/traefik/prod/traefik.yml"
      via: "configs section referencing traefik config file"
      pattern: "SPIN_MD5_HASH_TRAEFIK_YML"
---

<objective>
Create the three production infrastructure files: Docker Compose prod overlay with Swarm deployment, Traefik prod config with ACME SSL, and starter .spin.yml.

Purpose: These files enable `docker stack deploy` for production with automatic Let's Encrypt SSL, health checks, and zero-downtime rolling updates.
Output: template/docker-compose.prod.yml, template/.infrastructure/conf/traefik/prod/traefik.yml, template/.spin.yml
</objective>

<execution_context>
@/Users/juliandouma/.claude/get-shit-done/workflows/execute-plan.md
@/Users/juliandouma/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-production-and-ship/04-CONTEXT.md
@.planning/phases/04-production-and-ship/04-RESEARCH.md

<interfaces>
<!-- Existing base compose that prod overlay extends -->
From template/docker-compose.yml:
```yaml
services:
  traefik:
    image: traefik:v3.6
  php:
    depends_on:
      - traefik
```

From template/docker-compose.dev.yml (pattern reference for labels):
```yaml
labels:
  - "traefik.enable=true"
  - "traefik.http.routers.symfony.rule=HostRegexp(`localhost`)"
  - "traefik.http.routers.symfony.entrypoints=web"
  - "traefik.http.services.symfony.loadbalancer.server.port=8080"
  - "traefik.http.services.symfony.loadbalancer.server.scheme=http"
```

From post-install.sh (patching contract -- these exact strings must appear in prod files):
```bash
# post-install.sh searches for these exact strings:
"changeme@example.com"  # in traefik prod config AND .spin.yml
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create docker-compose.prod.yml with Swarm deployment config</name>
  <files>template/docker-compose.prod.yml</files>
  <read_first>
    - /Users/juliandouma/Developer/oss/spin/spin-template-laravel-basic/template/docker-compose.prod.yml
    - /Users/juliandouma/Developer/oss/spin-template-symfony/template/docker-compose.yml
    - /Users/juliandouma/Developer/oss/spin-template-symfony/template/docker-compose.dev.yml
    - /Users/juliandouma/Developer/oss/spin-template-symfony/.planning/phases/04-production-and-ship/04-RESEARCH.md
  </read_first>
  <action>
Create `template/docker-compose.prod.yml` as a compose overlay extending the base `docker-compose.yml`. Mirror the Laravel prod compose structure with these Symfony-specific adaptations:

**traefik service:**
- Ports: long-form syntax with `mode: host` for both 80 and 443 (NOT short-form `"80:80"`)
- Network: `web-public`
- Deploy block: `placement.constraints: [node.role==manager]`, `update_config` with `order: stop-first` (Traefik is stateful)
- Volumes: `/var/run/docker.sock:/var/run/docker.sock:ro` and `certificates:/certificates`
- Configs: `source: traefik`, `target: /etc/traefik/traefik.yml`

**php service:**
- Image: `${SPIN_IMAGE_DOCKERFILE}` (NOT `${SPIN_IMAGE_NAME}` -- match Laravel template exactly; add comment `# Change this if you're not using spin deploy`)
- Environment:
  - `APP_ENV: "prod"`
  - `PHP_OPCACHE_ENABLE: "1"`
  - `CADDY_SERVER_ROOT: "/var/www/html/public"`
  - `HEALTHCHECK_PATH: "/healthcheck"`
  - `SSL_MODE: "full"`
- Network: `web-public`
- Volume: `symfony_var:/var/www/html/var`
- Deploy block:
  - `replicas: 1`
  - `update_config`: `failure_action: rollback`, `parallelism: 1`, `delay: 5s`, `order: start-first`
  - `rollback_config`: `parallelism: 0`, `order: stop-first`
  - `restart_policy`: `condition: any`, `delay: 10s`, `max_attempts: 3`, `window: 120s`
  - `labels` (MUST be under `deploy.labels`, NOT top-level):
    - `traefik.enable=true`
    - `traefik.http.routers.symfony.rule=Host(\`${SPIN_APP_DOMAIN}\`)`
    - `traefik.http.routers.symfony.entrypoints=websecure`
    - `traefik.http.routers.symfony.tls=true`
    - `traefik.http.routers.symfony.tls.certresolver=letsencryptresolver`
    - `traefik.http.services.symfony.loadbalancer.server.port=8443` (FrankenPHP default; post-install.sh patches to 8080 for fpm-*)
    - `traefik.http.services.symfony.loadbalancer.server.scheme=https` (FrankenPHP default; post-install.sh patches to http for fpm-*)
    - `traefik.http.services.symfony.loadbalancer.healthcheck.path=/healthcheck`
    - `traefik.http.services.symfony.loadbalancer.healthcheck.interval=30s`
    - `traefik.http.services.symfony.loadbalancer.healthcheck.timeout=5s`
    - `traefik.http.services.symfony.loadbalancer.healthcheck.scheme=http`

**Top-level sections:**
- `configs:` with traefik config: `name: "traefik-${SPIN_MD5_HASH_TRAEFIK_YML}.yml"`, `file: ./.infrastructure/conf/traefik/prod/traefik.yml`
- `volumes:` declaring `certificates:` and `symfony_var:`
- `networks:` declaring `web-public:`

Use the router name `symfony` (NOT `my-php-app` from Laravel template). Use service name `symfony` in all Traefik label paths.
  </action>
  <verify>
    <automated>cd /Users/juliandouma/Developer/oss/spin-template-symfony && grep -q "deploy:" template/docker-compose.prod.yml && grep -q "node.role==manager" template/docker-compose.prod.yml && grep -q "SPIN_IMAGE_DOCKERFILE" template/docker-compose.prod.yml && grep -q "letsencryptresolver" template/docker-compose.prod.yml && grep -q "8443" template/docker-compose.prod.yml && grep -q "symfony_var" template/docker-compose.prod.yml && grep -q "SPIN_MD5_HASH_TRAEFIK_YML" template/docker-compose.prod.yml && grep -q "mode: host" template/docker-compose.prod.yml && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - File contains `deploy:` block with `placement.constraints`
    - File contains `image: ${SPIN_IMAGE_DOCKERFILE}`
    - File contains `APP_ENV: "prod"` and `PHP_OPCACHE_ENABLE: "1"`
    - File contains `loadbalancer.server.port=8443` and `loadbalancer.server.scheme=https`
    - File contains `healthcheck.path=/healthcheck`
    - File contains `tls.certresolver=letsencryptresolver`
    - File contains `mode: host` for Traefik ports
    - File contains `configs:` section with `SPIN_MD5_HASH_TRAEFIK_YML`
    - File contains `volumes:` with `certificates:` and `symfony_var:`
    - Labels are under `deploy.labels:` (NOT top-level `labels:`)
    - Router and service names use `symfony` (not `my-php-app`)
  </acceptance_criteria>
  <done>docker-compose.prod.yml exists with complete Swarm deployment config, FrankenPHP-default Traefik labels, health check, named volumes, and configs section</done>
</task>

<task type="auto">
  <name>Task 2: Create prod Traefik config and .spin.yml scaffold</name>
  <files>template/.infrastructure/conf/traefik/prod/traefik.yml, template/.spin.yml</files>
  <read_first>
    - /Users/juliandouma/Developer/oss/spin/spin-template-laravel-basic/template/.infrastructure/conf/traefik/prod/traefik.yml
    - /Users/juliandouma/Developer/oss/spin-template-symfony/.planning/phases/04-production-and-ship/04-RESEARCH.md
    - /Users/juliandouma/Developer/oss/spin-template-symfony/template/.infrastructure/conf/traefik/prod/.gitignore
  </read_first>
  <action>
**1. Replace `template/.infrastructure/conf/traefik/prod/.gitignore` stub with actual `traefik.yml`.**

Delete the `.gitignore` file in `template/.infrastructure/conf/traefik/prod/` and create `traefik.yml` in its place. Copy the Laravel template's prod traefik.yml nearly verbatim. The file must contain:

- YAML anchor `x-trustedIps: &trustedIPs` with all 22 Cloudflare IP ranges (copy exact list from Laravel reference)
- `serversTransport.insecureSkipVerify: true` (required for FrankenPHP self-signed internal TLS)
- `providers.swarm` (NOT `providers.docker`) with `endpoint: "unix:///var/run/docker.sock"` and `exposedByDefault: false`
- `entryPoints.web`: address `:80`, HTTP-to-HTTPS redirect (`redirections.entrypoint.to: websecure`, `scheme: https`), `forwardedHeaders.trustedIPs` and `proxyProtocol.trustedIPs` using `*trustedIPs` anchor
- `entryPoints.websecure`: address `:443`, same `forwardedHeaders` and `proxyProtocol` with `*trustedIPs`
- `accessLog: {}`
- `log.level: ERROR`
- `api.dashboard: true`, `api.insecure: true`
- `certificatesResolvers.letsencryptresolver.acme`:
  - `email: "changeme@example.com"` (MUST be this exact string -- post-install.sh patches it)
  - `storage: "/certificates/acme.json"`
  - `httpChallenge.entryPoint: web`

**2. Create `template/.spin.yml`.**

Minimal scaffold with comments. Must contain:

```yaml
# .spin.yml - Spin server configuration
# Full documentation: https://serversideup.net/open-source/spin/docs/server-configuration/spin-yml-usage

server_contact: changeme@example.com
server_timezone: "Etc/UTC"
use_passwordless_sudo: true

users:
  - username: spin
    name: Spin User
    groups: ['sudo']
    authorized_keys:
      - public_key: "~/.ssh/id_ed25519.pub"

servers:
  - server_name: web-1
    environment: production
    hardware_profile: your_hardware_profile

environments:
  - name: production
```

The `server_contact` value MUST be exactly `changeme@example.com` -- post-install.sh uses `line_in_file --action exact` to patch this.
  </action>
  <verify>
    <automated>cd /Users/juliandouma/Developer/oss/spin-template-symfony && grep -q "swarm" template/.infrastructure/conf/traefik/prod/traefik.yml && grep -q "insecureSkipVerify: true" template/.infrastructure/conf/traefik/prod/traefik.yml && grep -q "changeme@example.com" template/.infrastructure/conf/traefik/prod/traefik.yml && grep -q "173.245.48.0/20" template/.infrastructure/conf/traefik/prod/traefik.yml && grep -q "letsencryptresolver" template/.infrastructure/conf/traefik/prod/traefik.yml && grep -q "websecure" template/.infrastructure/conf/traefik/prod/traefik.yml && grep -q "changeme@example.com" template/.spin.yml && grep -q "server_contact" template/.spin.yml && grep -q "environments" template/.spin.yml && ! test -f template/.infrastructure/conf/traefik/prod/.gitignore && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `template/.infrastructure/conf/traefik/prod/traefik.yml` exists with `providers.swarm` (not `providers.docker`)
    - Traefik config contains `insecureSkipVerify: true`
    - Traefik config contains `changeme@example.com` in ACME email field
    - Traefik config contains all 22 Cloudflare IP ranges (first: `173.245.48.0/20`, last: `2c0f:f248::/32`)
    - Traefik config has HTTP-to-HTTPS redirect in web entrypoint
    - Traefik config has `certificatesResolvers.letsencryptresolver.acme.httpChallenge`
    - `template/.infrastructure/conf/traefik/prod/.gitignore` is removed (replaced by traefik.yml)
    - `template/.spin.yml` exists with `server_contact: changeme@example.com`
    - `.spin.yml` contains `users`, `servers`, `environments` sections
  </acceptance_criteria>
  <done>Prod Traefik config with ACME HTTP-01 and Cloudflare IPs replaces the .gitignore stub; .spin.yml scaffold ready for post-install.sh email patching</done>
</task>

</tasks>

<verification>
All three infrastructure files exist and contain correct configuration:
```bash
cd /Users/juliandouma/Developer/oss/spin-template-symfony
# Prod compose validates
grep -c "deploy:" template/docker-compose.prod.yml  # Should be >= 2 (traefik + php)
# Traefik uses Swarm provider
grep "swarm" template/.infrastructure/conf/traefik/prod/traefik.yml
# ACME email placeholder matches post-install.sh contract
grep "changeme@example.com" template/.infrastructure/conf/traefik/prod/traefik.yml template/.spin.yml
# FrankenPHP defaults in prod compose
grep "8443" template/docker-compose.prod.yml
```
</verification>

<success_criteria>
- docker-compose.prod.yml creates a valid Swarm deployment overlay for the base compose
- Traefik prod config enables Let's Encrypt with HTTP-01 challenge and Cloudflare IP trust
- .spin.yml provides a minimal scaffold with the changeme@example.com contract string
- All files use the `symfony` router/service name (not `my-php-app`)
</success_criteria>

<output>
After completion, create `.planning/phases/04-production-and-ship/04-01-SUMMARY.md`
</output>
